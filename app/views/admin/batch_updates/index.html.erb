<div class="container mx-auto px-6 py-10 bg-black/50 rounded-xl backdrop-blur-sm">
  <div class="mb-8">
    <h1 class="text-4xl font-bold text-white mb-2">🤖 Batch Update History</h1>
    <p class="text-gray-300">Complete history of automated playlist updates</p>
    
    <!-- Summary Stats -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-6">
      <div class="bg-gradient-to-br from-blue-500/20 to-blue-600/20 rounded-xl p-4 border border-blue-500/30">
        <div class="text-blue-400 text-sm font-medium">Total Runs</div>
        <div class="text-2xl font-bold text-white"><%= @total_batches %></div>
      </div>
      <div class="bg-gradient-to-br from-green-500/20 to-green-600/20 rounded-xl p-4 border border-green-500/30">
        <div class="text-green-400 text-sm font-medium">Successful</div>
        <div class="text-2xl font-bold text-white"><%= @successful_batches %></div>
      </div>
      <div class="bg-gradient-to-br from-red-500/20 to-red-600/20 rounded-xl p-4 border border-red-500/30">
        <div class="text-red-400 text-sm font-medium">Failed</div>
        <div class="text-2xl font-bold text-white"><%= @failed_batches %></div>
      </div>
      <div class="bg-gradient-to-br from-yellow-500/20 to-yellow-600/20 rounded-xl p-4 border border-yellow-500/30">
        <div class="text-yellow-400 text-sm font-medium">Total Changes</div>
        <div class="text-2xl font-bold text-white"><%= @total_changes %></div>
      </div>
    </div>
    
    <!-- Last 24h Stats -->
    <div class="mt-4 p-4 bg-gradient-to-r from-purple-900/30 to-blue-900/30 rounded-lg border border-purple-500/30">
      <div class="flex items-center justify-between">
        <div>
          <h3 class="text-lg font-bold text-purple-400 mb-1">📊 Last 24 Hours</h3>
          <p class="text-gray-300 text-sm"><%= @last_24h_changes %> changes detected</p>
        </div>
        <div class="text-right">
          <p class="text-purple-400 text-sm font-semibold">
            <% if @last_24h_changes > 0 %>
              ✅ Changes Found
            <% else %>
              ⚠️ No Changes
            <% end %>
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- Batch Updates Table -->
  <div class="bg-gray-900/50 rounded-xl border border-gray-700 overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-700">
      <h2 class="text-xl font-bold text-white">Recent Batch Updates</h2>
    </div>
    
    <div class="overflow-x-auto">
      <table class="w-full">
        <thead class="bg-gray-800/50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Status</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Started</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Duration</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Changes</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Progress</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Source</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-700">
          <% @batch_updates.each do |batch| %>
            <tr class="hover:bg-gray-800/30 transition-colors">
              <td class="px-6 py-4 whitespace-nowrap">
                <% case batch.status %>
                <% when 'completed' %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                    ✅ Completed
                  </span>
                <% when 'running' %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800 animate-pulse">
                    🔄 Running
                  </span>
                <% when 'failed' %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                    ❌ Failed
                  </span>
                <% else %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                    ⏸️ <%= batch.status&.titleize %>
                  </span>
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <div><%= batch.started_at&.strftime("%m/%d %H:%M:%S") %></div>
                <div class="text-xs text-gray-500"><%= time_ago_in_words(batch.started_at) %> ago</div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <% if batch.completed_at && batch.started_at %>
                  <%= ((batch.completed_at - batch.started_at) / 60).round(1) %>m
                <% elsif batch.started_at %>
                  <span class="text-yellow-400">Running...</span>
                <% else %>
                  <span class="text-gray-500">N/A</span>
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <span class="font-semibold text-white"><%= batch.changes_count || 0 %></span>
                <span class="text-xs text-gray-500">changes</span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <% if batch.total_count && batch.total_count > 0 %>
                  <div class="flex items-center">
                    <div class="w-16 bg-gray-700 rounded-full h-2 mr-2">
                      <div class="bg-green-600 h-2 rounded-full" style="width: <%= (batch.current_index.to_f / batch.total_count * 100).round(1) %>%"></div>
                    </div>
                    <span class="text-xs"><%= batch.current_index %>/<%= batch.total_count %></span>
                  </div>
                <% else %>
                  <span class="text-gray-500">N/A</span>
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <% if batch.source == 'heroku_scheduler' %>
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-purple-100 text-purple-800">
                    🤖 Automated
                  </span>
                <% else %>
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-blue-100 text-blue-800">
                    👤 Manual
                  </span>
                <% end %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                <%= link_to "View Details", admin_batch_update_path(batch), 
                    class: "text-green-400 hover:text-green-300 font-medium" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    
    <!-- Simple Info -->
    <div class="px-6 py-4 border-t border-gray-700">
      <div class="text-sm text-gray-400 text-center">
        Showing latest <%= @batch_updates.count %> batch updates
      </div>
    </div>
  </div>
  
  <!-- Back to Dashboard -->
  <div class="mt-6">
    <%= link_to "← Back to Dashboard", admin_root_path, 
        class: "inline-flex items-center px-4 py-2 bg-gray-700 text-white rounded-lg hover:bg-gray-600 transition-colors" %>
  </div>
</div>
